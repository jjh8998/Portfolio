using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// 속성 : 물 > 불 > 풀 > 물

public enum Attribute
{
    Nothing, // 무속성
    Fire, // 불속성
    Water, // 물속성
    Grass, // 풀속성
    ETC // 장비 아이템 아님
}
